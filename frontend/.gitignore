# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Not secret (no client secret, gateway URL is inherently public), but the EC2 IP
# changes on every stop/start -- keeping this out of git avoids committing a value
# that goes stale. Copy .env.example -> .env and fill in your current values.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
